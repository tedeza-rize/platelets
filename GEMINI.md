**STOP — all agent rules live in `AGENTS.md`, imported below. Read it in full before any edit and follow it exactly.** Above all: NEVER commit directly to `main` — always create a branch and open a PR, even for one-line or docs-only changes.

@./AGENTS.md
